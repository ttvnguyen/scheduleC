<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs') %>
<%- include('./partials/header.ejs') %>
<body>
    <div class="container" >
        <table>
            <tr>
                <th>Description</th>
                <th>Category</th>
                <th>Amount</th>
                <th></th>
                <th></th>
            </tr>
            <% for (let i=0; i<transactions.length;i++){%>
                <tr>
                    <th>
                        <a href="/transactions/<%= transactions[i]._id %>"><%= transactions[i].description%>&nbsp;</a></th>
                    <th><%= transactions[i].category%></th>
                    <th><%= transactions[i].amount%></th>
                    <th><form action="/transactions/<%= transactions[i]._id%>?_method=DELETE" method="POST">
                        <input type="submit" class="fa-solid fa-trash-can" value="&#xf014;" >
                    </form></th>
                    <th><a href="/transactions/<%= transactions[i]._id %>/edit" class="fa-solid fa-edit"></a></th>
                </tr>
            <%}%>
        </table>
    </div>
    <%- include ('./partials/footer.ejs') %>
</body>
</html>